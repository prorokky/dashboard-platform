# Platform UI Design System

`platform-ui` is a shared design system for all microfrontends in this repository.

## Structure

- `src/styles/tokens.css` - global design tokens
- `src/styles/base.css` - base reset and typography defaults
- `src/components` - reusable UI primitives
- `src/index.ts` - public component exports

